class Solution(object):
    def generate(self, numRows):
        x = [] 

        for j in range(numRows):
            y = [1] * (j + 1) 
            for i in range(1, j):
                y[i] = x[j - 1][i - 1] + x[j - 1][i]

            x.append(y)

        return x
