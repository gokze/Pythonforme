class Solution:
    def reverseBits(self, n: int) -> int:
        a=bin(n)
        a=a[2::1]
        if(len(a)!=32):
            for i in range(0,32-len(a)):
                a="0"+a
        print(a)
        a=a[-1::-1]
        print(a)
        return int(a,2)
